# Strapi (server & image URLs). Required for Vercel/serverless fetches.
STRAPI_URL=http://144.24.219.37:1337

# Optional: same value — exposes URL to the browser for media paths in client components.
# Set this if your Strapi base URL differs from STRAPI_URL or you use HTTPS in production.
# NEXT_PUBLIC_STRAPI_URL=http://144.24.219.37:1337
